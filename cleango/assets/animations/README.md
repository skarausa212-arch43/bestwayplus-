# /assets/animations

The SPA ships motion in CSS (no external animation libraries): reveal/stagger,
count-up, confetti, the LUMI Score ring draw, typing dots and page transitions —
see the `@keyframes` in `../../public/index.html`. Motion honours
`prefers-reduced-motion`. Export Lottie/`.json` here when the native apps need
shared timelines.
