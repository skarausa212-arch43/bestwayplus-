# /assets/ui

Reference exports of the shared component set. The **canonical, living**
components render in `../../design/components.html` and ship in the SPA
(`../../public/index.html`) — these files document the inventory, they are not a
second source of truth (29 §"Never duplicate business logic / shared components
only").

Components: Buttons, Inputs, Cards, Chips, Dialogs, Bottom Sheets, Skeletons.
