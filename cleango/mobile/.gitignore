node_modules/
www/
ios/
android/
*.log
.DS_Store
# Your Firebase client configs — keep local (build copies them into the projects)
google-services.json
GoogleService-Info.plist

# generated by `npm run assets` alongside the native icons — the site has its own
icons/
