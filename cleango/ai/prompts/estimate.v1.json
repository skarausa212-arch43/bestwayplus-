{
  "id": "estimate",
  "version": "v1",
  "model": "lumi-heuristic",
  "purpose": "Assist booking price by estimating duration, workers and difficulty. Advisory only — the backend pricing engine owns the authoritative total (10_AI_ARCHITECTURE.md · Price Assistance).",
  "inputSchema": {
    "service": "string", "rooms": "int", "baths": "int", "area": "number", "extras": "string[]", "urgency": "enum(scheduled,today,flash)", "city": "string"
  },
  "outputSchema": {
    "estimatedDurationMinutes": "int",
    "estimatedWorkers": "int",
    "difficultyScore": "number(0..100)",
    "confidence": "number(0..1)"
  },
  "notes": "Never charge from AI output alone. Low confidence -> safer default pricing."
}
