{
  "id": "concierge",
  "version": "v1",
  "model": "lumi-heuristic",
  "purpose": "Turn a natural-language home situation into a ready-to-book suggestion (bundle of service + extras + urgency).",
  "inputSchema": { "text": "string" },
  "outputSchema": { "service": "string", "extras": "string[]", "urgency": "enum", "title": "string", "reason": "string", "confidence": "number(0..1)" },
  "notes": "Advisory. User confirms in the wizard before any charge."
}
